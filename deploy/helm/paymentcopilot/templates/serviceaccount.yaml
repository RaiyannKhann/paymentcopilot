{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "paymentcopilot.serviceAccountName" . }}
  labels:
    {{- include "paymentcopilot.labels" . | nindent 4 }}
{{- end }}
